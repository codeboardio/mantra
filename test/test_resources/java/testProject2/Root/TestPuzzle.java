class TestPuzzle {
    
    public void runTests() {
        // Add here your test cases using the class Test
        // To invoke a test, write t.run(array, integer)
        // For example
        
        Test t = new Test();
        String [] aa;
        aa = new String[]{"ala", "rotator","renner", "civic"};
        t.run(aa,0);
 
        aa = new String[]{"ala", "rotator","renner", "civic"};
        t.run(aa,4); 
    }
}